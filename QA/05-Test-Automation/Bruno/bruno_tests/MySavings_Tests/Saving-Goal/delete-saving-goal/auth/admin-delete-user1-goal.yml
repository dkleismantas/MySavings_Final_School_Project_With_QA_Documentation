info:
  name: admin-delete-user1-goal
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
