info:
  name: user2-delete-user1-goal
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{user2Token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
