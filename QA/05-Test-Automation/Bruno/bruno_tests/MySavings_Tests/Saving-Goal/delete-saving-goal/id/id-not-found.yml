info:
  name: id-not-found
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
