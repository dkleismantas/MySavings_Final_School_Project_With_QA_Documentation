info:
  name: id-boolean
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/true"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
