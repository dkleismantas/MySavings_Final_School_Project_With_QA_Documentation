info:
  name: id-zero
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/0"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: "[400, 404]"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
