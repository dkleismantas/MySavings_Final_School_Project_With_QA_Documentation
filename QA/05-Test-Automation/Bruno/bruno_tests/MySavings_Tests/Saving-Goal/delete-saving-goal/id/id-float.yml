info:
  name: id-float
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/4.5"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
