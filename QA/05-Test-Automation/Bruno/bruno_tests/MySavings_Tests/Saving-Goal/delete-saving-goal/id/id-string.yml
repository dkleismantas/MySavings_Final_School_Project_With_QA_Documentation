info:
  name: id-string
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/api/SavingGoal/delete-saving-goal/abc"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
