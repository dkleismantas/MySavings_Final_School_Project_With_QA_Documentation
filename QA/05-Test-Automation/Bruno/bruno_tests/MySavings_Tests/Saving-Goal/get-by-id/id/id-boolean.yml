info:
  name: id-boolean
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/true"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: "[400, 404]"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
