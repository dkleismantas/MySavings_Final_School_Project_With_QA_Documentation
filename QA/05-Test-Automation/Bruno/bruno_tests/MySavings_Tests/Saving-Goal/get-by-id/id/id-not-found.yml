info:
  name: id-not-found
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/999999"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
