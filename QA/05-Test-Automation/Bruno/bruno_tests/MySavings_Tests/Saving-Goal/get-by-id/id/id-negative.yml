info:
  name: id-negative
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/-1"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: "[400, 404]"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
