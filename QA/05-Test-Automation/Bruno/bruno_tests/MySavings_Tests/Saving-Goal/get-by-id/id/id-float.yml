info:
  name: id-float
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/4.5"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: "[400, 404]"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
