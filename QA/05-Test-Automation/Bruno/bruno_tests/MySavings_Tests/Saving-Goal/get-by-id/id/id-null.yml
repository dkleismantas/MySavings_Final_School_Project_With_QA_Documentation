info:
  name: id-null
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/null"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: in
      value: "[400, 404]"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
