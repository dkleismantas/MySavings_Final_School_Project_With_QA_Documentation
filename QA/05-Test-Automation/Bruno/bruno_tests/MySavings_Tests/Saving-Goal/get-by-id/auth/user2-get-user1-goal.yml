info:
  name: user2-get-user1-goal
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{user2Token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
