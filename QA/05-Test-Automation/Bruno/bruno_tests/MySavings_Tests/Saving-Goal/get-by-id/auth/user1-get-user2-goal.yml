info:
  name: user1-get-user2-goal
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/{{user2_savingGoalId}}"
  auth:
    type: bearer
    token: "{{user1Token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
