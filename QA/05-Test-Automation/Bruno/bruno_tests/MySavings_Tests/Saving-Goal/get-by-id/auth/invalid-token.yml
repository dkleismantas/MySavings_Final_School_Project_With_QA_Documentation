info:
  name: invalid-token
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-by-id/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{invalidToken}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
