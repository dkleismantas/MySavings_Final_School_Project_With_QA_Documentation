info:
  name: sort-unknown
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-saving-goals/?sortBy=xxx"
  params:
    - name: sortBy
      value: xxx
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
