info:
  name: direction-invalid
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/SavingGoal/get-saving-goals?sortDirection=wrong"
  params:
    - name: sortDirection
      value: wrong
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
