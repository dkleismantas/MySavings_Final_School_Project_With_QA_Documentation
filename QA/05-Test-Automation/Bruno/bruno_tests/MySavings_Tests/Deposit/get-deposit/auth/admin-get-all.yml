info:
  name: admin-get-all
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/Deposit"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
