info:
  name: admin-access-user1-goal
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/Deposit/goal/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{adminToken}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
