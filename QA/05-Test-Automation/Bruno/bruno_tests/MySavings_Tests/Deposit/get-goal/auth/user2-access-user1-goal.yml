info:
  name: user2-access-user1-goal
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/Deposit/goal/{{user1_savingGoalId}}"
  auth:
    type: bearer
    token: "{{user2Token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
