info:
  name: user1-access-user2-goal
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/Deposit/goal/{{user2_savingGoalId}}"
  auth:
    type: bearer
    token: "{{user1Token}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "403"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
