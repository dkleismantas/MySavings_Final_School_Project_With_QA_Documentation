info:
  name: id-boolean
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/Deposit/goal/true"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
